# Results

Per-experiment JSON dumps produced by `scripts/evaluate.py --out-json`.
Each file holds mIoU, per-class IoU, and a reference to the checkpoint used.

Trained model weights (`*.pt`) are not kept under version control because
they are too large for GitHub. Reproduce them by running the corresponding
training command from the project README.
